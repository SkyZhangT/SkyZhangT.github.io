import React from "react";
import { Typography } from "@material-ui/core";
import { makeStyles, useTheme } from "@material-ui/core/styles";

const Gallery = (props) => {
  return (
    <main>
      <Typography variant="h1">This is gallery</Typography>
    </main>
  );
};

export default Gallery;
