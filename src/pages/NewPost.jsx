import React from "react";
import { Typography } from "@material-ui/core";

const NewPost = (props) => {
  return (
    <main>
      <Typography variant="h1">This is newpost</Typography>
    </main>
  );
};

export default NewPost;
